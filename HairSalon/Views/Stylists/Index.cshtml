@{
  Layout = "_Layout";
}

@using HairSalon.Models

<h1>List of Stylists:</h1>

@if( Model.Count == 0 )
{
  <h3>No stylists have been added yet!</h3>
}
else
{
  @foreach (Stylist stylist in Model)
  {
    <li>stylist.Name</li>
  }
}

@Html.ActionLink("Add a stylist", "Create")
